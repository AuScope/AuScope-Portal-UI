export interface GraceStyleSettings {
    minColor: string;
    minValue: number;
    neutralColor: string;
    neutralValue: number;
    maxColor: string;
    maxValue: number;
    transparentNeutralColor: boolean;
}
