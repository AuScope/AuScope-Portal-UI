{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Angular Debug",
            "type": "pwa-chrome",
            "request": "launch",
            "preLaunchTask": "npm start",
            "url": "http://localhost:4200",
            "webRoot": "${workspaceFolder}"
        }
    ]
}